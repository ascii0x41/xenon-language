sure. generate the .cpp files. then i can get started on the actual semantic analyser. btw i need some sort of table for module exports because i plan on having some sort of global context like:

```
class GlobalContext {
	ExportTable export_table_; // std::unordered_map
public:
	GlobalContext() = default;

	// If the module name starts with "std/" then it looks for the stdlib
	// Every file must fetch "std/builtins"
	const ModuleExport fetch_module_export(const std::string& module_name) const;

	// The modules are in reverse order where idx -1 is the entry module
	// Goal is to fill th	
	void populate_export_table(const std::vector<Module>& modules);
};

class FileValidator {
	Module& module;
public:
    static bool validate(const Module& mod);
};

bool validate_project() {
    GlobalContext g_context;
    bool success = true;

    g_context.populate_export_table();
    
    for (const auto& mod : modules_) {
        // This really shouldnt fail, bc of the ERROR builtin type
        // But just in case...
        try {
            if (!FileValidator::validate(mod)) success = false;
        } catch (const ComplierException& e) {
            success = false;
        }
    }
}
